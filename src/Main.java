import java.io.*;import java.util.ArrayList;import java.util.List;public class Main {    public static void main(String[] Args) throws FileNotFoundException {        WordPlacement WP = new WordPlacement();        try (BufferedReader br = new BufferedReader(new FileReader("src/loadBoard"))) {            boolean out = WP.loadBoard(br);            System.out.println(out);        } catch (IOException e) {            e.printStackTrace();        }        try (BufferedReader br = new BufferedReader(new FileReader("src/letterValue"))) {            boolean out = WP.letterValue(br);            System.out.println(out);        } catch (IOException e) {            e.printStackTrace();        }        try (BufferedReader br = new BufferedReader(new FileReader("src/Dictionary"))) {            boolean out = WP.dictionary(br);            System.out.println(out);        } catch (IOException e) {            e.printStackTrace();        }        ArrayList<String> words = new ArrayList<String>();        words.add("cat");         words.add("let");        //words.add("tea");        WP.placeWords(words);        String File = "src/PuzzleOutput";        try {            FileWriter fw = new FileWriter(File);            PrintWriter outstream = new PrintWriter(fw);            WP.print(outstream);            outstream.close();            fw.close();        } catch (IOException e) {            throw new RuntimeException(e);        }    }}